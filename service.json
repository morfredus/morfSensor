{
  "_comment": "Deployment facts for this project, read by morfdeploy. The four install steps live in one place for the whole parc; this file states only what is specific to this service.",
  "service_name": "morfsensor",
  "display_name": "morfSensor",
  "description": "Sensor acquisition service",
  "binary": "morfsensor",
  "_comment_app_dir": "Overridden by MORF_APP_DIR, one variable for every service. The old per-project spellings (MT_/MN_/MS_) are still honoured with a notice.",
  "app_dir": {
    "linux": "/opt/morfsensor",
    "windows": "%ProgramData%/morfsensor"
  },
  "_comment_configs": "Copied at install. Never overwritten by default: these hold settings edited by hand on the machine. An absolute destination is honoured as-is, which is how the shared parc file reaches /etc/morfsystem.",
  "configs": [
    {
      "source": "config/morfsensor.example.json",
      "dest": "morfsensor.json",
      "migrate_from": [
        "/etc/morfsensor/morfsensor.json",
        "/opt/morfsensor/morfsensor.json"
      ]
    }
  ],
  "status_url": "http://127.0.0.1:8788/status",
  "_comment_config_dir": "La configuration vit sous /etc/morfsystem/<service> : un point d'entree UNIQUE dans /etc pour tout le parc, et non a cote du binaire dans /opt. Effacer /opt/<service> pour reinstaller proprement n'emporte donc pas les reglages.",
  "config_dir": {
    "linux": "/etc/morfsystem/morfsensor",
    "windows": "%ProgramData%/morfsystem/morfsensor"
  },
  "_comment_system_dependencies": "Dependances systeme declarees comme BESOINS (morfDeploy resout le paquet selon la plateforme). Qt SerialPort est OPTIONNELLE : sans elle, morfSensor compile et tourne (coeur + capteurs simules), seul le driver radar LD2410C est desactive (le CMakeLists le detecte). Sur une machine devant piloter un vrai LD2410C, installer le paquet AVANT le build ; morfDeploy le proposera lors de deploy/install.",
  "system_dependencies": [
    {
      "id": "qt-serialport",
      "label": "Qt SerialPort",
      "required_for": ["ld2410c"],
      "packages": { "debian": ["qt6-serialport-dev"] },
      "required": false
    }
  ]
}
